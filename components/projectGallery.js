const projectGalleryAvt = ({ img }) => {
    return `<img class="img_homePage" src="${img}" />`
}

const projectGallerySplash = ({ img }) => {
    return `<img class="homePage_row_right-splash" src="${img}" />`
}

export { projectGalleryAvt, projectGallerySplash }
